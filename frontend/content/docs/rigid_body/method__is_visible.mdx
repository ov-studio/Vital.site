---
title: self:is_visible
description: Checks whether the rigid body is visible
badge: client
---

### Syntax

```lua
local result = self:is_visible()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | result | `true` if visible, `false` otherwise |

---

### Examples

```lua lineNumbers title="Check visibility of a rigid body"
local entity = physics.rigid.create()

core.engine.print("info", "Visible:", entity:is_visible())
```
