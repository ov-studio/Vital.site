---
title: self:get_quaternion
description: Retrieves the local rotation of the rigid body as a quaternion
badge: client
---

### Syntax

```lua
local quaternion = self:get_quaternion()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector4` | quaternion | Current rotation as `{x, y, z, w}` |

---

### Examples

```lua lineNumbers title="Get the quaternion of a rigid body"
local entity = physics.rigid.create()
local quaternion = entity:get_quaternion()

core.engine.print("info", "Quaternion:", util.table.unpack(quaternion))
```
