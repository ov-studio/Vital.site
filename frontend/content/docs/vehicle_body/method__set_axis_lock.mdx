---
title: self:set_axis_lock
description: Locks or unlocks a specific movement axis on the vehicle body
badge: client
---

### Syntax

```lua
local status = self:set_axis_lock(
    axis,
    lock
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `int` | axis | Axis enum value — refer to [`physics.vehicle.axis`](/docs/vehicle_body/enum/axis) |
| `bool` | lock | `true` to lock the axis, `false` to unlock it |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Lock an axis on a vehicle body"
local entity = physics.vehicle.create()

entity:set_axis_lock(physics.vehicle.axis.LINEAR_X, true)
```
