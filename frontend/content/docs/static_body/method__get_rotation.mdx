---
title: self:get_rotation
description: Retrieves the local rotation of the static body in degrees
badge: client
---

### Syntax

```lua
local rotation = self:get_rotation()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector3` | rotation | Current local rotation in degrees as `{x, y, z}` |

---

### Examples

```lua lineNumbers title="Get the local rotation of a static body"
local entity = physics.static.create()
local rotation = entity:get_rotation()

core.engine.print("info", "Static body rotation:", util.table.unpack(rotation))
```
