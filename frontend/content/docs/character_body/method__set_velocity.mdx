---
title: self:set_velocity
description: Sets the velocity of the character body, used as the input for the next `move_and_slide` call
badge: client
---

### Syntax

```lua
local status = self:set_velocity(
    velocity
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | velocity | Velocity to apply as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Set the velocity of a character body"
local entity = physics.character.create()

entity:set_velocity({0, 0, -5})
```
