---
title: self:is_visible
description: Checks whether the area is visible
badge: client
---

### Syntax

```lua
local result = self:is_visible()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | result | `true` if visible, `false` otherwise |

---

### Examples

```lua lineNumbers title="Check visibility of a area"
local entity = physics.area.create()

core.engine.print("info", "Visible:", entity:is_visible())
```
