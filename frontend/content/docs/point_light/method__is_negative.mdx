---
title: self:is_negative
description: Checks whether the point light is set to negative mode, which subtracts light from the scene
badge: client
---

### Syntax

```lua
local result = self:is_negative()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | result | `true` if the light is in negative mode, `false` otherwise |

---

### Examples

```lua lineNumbers title="Check is negative on a point light"
local entity = light.point.create()

core.engine.print("info", "Point light negative:", entity:is_negative())
```
