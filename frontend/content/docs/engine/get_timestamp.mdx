---
title: core.engine.get_timestamp
description: Retrieves current timestamp
badge: shared
---

### Syntax

```lua
local timestamp = core.engine.get_timestamp()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `table` | timestamp | Current timestamp containing the following fields:<br/>• `hour` (number) - current hour<br/>• `minute` (number) - current minute<br/>• `second` (number) - current second<br/>• `day` (number) - current day<br/>• `month` (number) - current month<br/>• `year` (number) - current year |

---

### Examples

```lua lineNumbers title="Prints current timestamp"
core.engine.print("info", "current timestamp:")
core.engine.iprint(core.engine.get_timestamp())
```
