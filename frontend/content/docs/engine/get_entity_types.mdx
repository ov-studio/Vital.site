---
title: core.engine.get_entity_types
description: Retrieves all currently registered entity category names
badge: shared
---

### Syntax

```lua
local result = core.engine.get_entity_types()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `table` | result | An array of available entity category names<br/>Each entry is usable as the `category` argument to [`core.engine.get_entities`](/docs/engine/get_entities) |

---

### Entity Types

| Category | Availability | Streamable | Notes |
|----------|-------------|-----------|-------|
| `core.model` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `core.audio_3d` | `client` | ✓ | Frustum-culled positional audio |
| `core.audio_2d` | `client` | — | |
| `core.camera` | `client` | — | |
| `core.image` | `client` | — | |
| `core.shader` | `client` | — | |
| `core.svg` | `client` | — | |
| `core.font` | `client` | — | |
| `core.rendertarget` | `client` | — | |
| `core.webview` | `client` | — | |
| `core.database` | `server` | — | |
| `core.db_query` | `server` | — | |
| `light.point` | `client` | ✓ | Frustum-culled |
| `light.spot` | `client` | ✓ | Frustum-culled |
| `light.area` | `client` | ✓ | Frustum-culled |
| `light.directional` | `client` | ✓ | Always streamed (no position) |
| `physics.rigid` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.static` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.character` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.animatable` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.vehicle` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.vehicle_wheel` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.collision_shape` | `client` \| `server` | ✓ | Frustum-culled. Supports `is_remote()` |
| `physics.area` | `client` \| `server` | — | |
| `util.promise` | `client` \| `server` | — | |
| `util.thread` | `client` \| `server` | — | |
| `util.timer` | `client` \| `server` | — | |

---

### Examples

```lua lineNumbers title="Prints all registered entity categories"
core.engine.iprint(core.engine.get_entity_types())
```

```lua lineNumbers title="Counts entities across every category"
for _, category in ipairs(core.engine.get_entity_types()) do
    core.engine.print("info", category, "count:", #core.engine.get_entities(category))
end
```

```lua lineNumbers title="Registers a monitor for every entity category"
for _, category in ipairs(core.engine.get_entity_types()) do
    util.monitor.register(category .. "_count", util.string.upper(category) .. "_COUNT", function() 
        return #core.engine.get_entities(category)
    end, util.monitor.stat_format.QUANTITY)
end
```