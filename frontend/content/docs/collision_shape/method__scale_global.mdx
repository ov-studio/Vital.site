---
title: self:scale_global
description: Scales the collision shape in the global coordinate space
badge: client
---

### Syntax

```lua
local status = self:scale_global(
    scale
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | scale | Scale factor to apply as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Scale a collision shape globally"
local entity = physics.collision_shape.create()

entity:scale_global({2, 2, 2})
```
