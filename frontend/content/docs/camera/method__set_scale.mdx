---
title: self:set_scale
description: Sets the local scale of the camera
badge: client
---

### Syntax

```lua
local status = self:set_scale(
    scale
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | scale | Local scale to apply as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Set the local scale of a camera"
local entity = core.camera.create()

entity:set_scale({1, 1, 1})
```
