---
title: self:get_rotation
description: Retrieves the local rotation of the spot light
badge: client
---

### Syntax

```lua
local rotation = self:get_rotation()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector3` | rotation | Current local rotation in degrees as `{x, y, z}` |

---

### Examples

```lua lineNumbers title="Get the local rotation of a spot light"
local entity = light.spot.create()
local rotation = entity:get_rotation()

core.engine.print("info", "Spot light rotation:", util.table.unpack(rotation))
```
