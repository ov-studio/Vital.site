---
title: self:translate_local
description: Translates the vehicle wheel by an offset in its own local coordinate space
badge: client
---

### Syntax

```lua
local status = self:translate_local(
    offset
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | offset | Offset to translate by as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Translate a vehicle wheel locally"
local entity = physics.vehicle_wheel.create()

entity:translate_local({0, 0, -1})
```
