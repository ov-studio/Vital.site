---
title: event.emit_callback
description: Fires an event and returns a promise that resolves with the handler's return values
badge: Shared
---

### Syntax

```lua
local result = event.emit_callback(
    name,
    options = nil,
    ...
)
```

<Callout type="info">
  **`options` is positional — pass `nil` or omit it entirely when no routing is needed.**

  - **When routing options are needed** — pass the options table as the second argument
  - **When no routing options are needed** — omit `options` and pass arguments directly after `name`
</Callout>

<Callout type="warn">
  **Only serializable values survive remote transmission. Non-serializable values are silently dropped.**

  - **Safe to send** — `nil`, `bool`, `number`, `string`, `table` (including nested tables)
  - **Silently dropped** — functions, userdata, threads, and any other non-serializable Lua values
  - **Local-only emit** (`remote = false`) — no serialization occurs; all Lua values are forwarded as-is
  - **Nested tables** — fully supported, but deeply nested structures increase payload size and serialization cost
</Callout>

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `string` | name | Name of the event to fire |
| `table` | options | Routing options table<br/>Refer [Options](#options) section |
| `any` | ... | Arguments forwarded to every handler |

### Returns

Always returns a **single** `promise` regardless of how many handlers are registered or whether the call is local or remote.<br/>
For remote calls, the remote side aggregates all of its handlers using the same rules before sending a single reply — so the caller's promise resolves with the same structure described below.

How that promise resolves depends on the number of registered handlers:

| Handlers | Resolved value |
|----------|----------------|
| None | Resolves immediately with no values |
| One | Resolves directly with that handler's return value(s), unpacked |
| Multiple | Resolves with `{[1]={...}, [2]={...}, ...}` — each slot is always a table of that handler's return values, even if only one value was returned |

---

<include>./emit.mdx#options</include>

### Examples

```lua lineNumbers title="Await a return value from a single local handler"
event.on("on_get_score", function()
    return 42
end)

thread.create(function(self)
    local ok, score = self:await(event.emit_callback("on_get_score"))
    engine.print("info", "Score:", score) -- 42
end):resume()
```

```lua lineNumbers title="Collect return values from multiple local handlers"
event.on("on_query", function() return "handler_a" end)
event.on("on_query", function() return "handler_b" end)

thread.create(function(self)
    local ok, results = self:await(event.emit_callback("on_query"))
    engine.print("info", "Handler 1 returned:", results[1][1]) -- 'handler_a'
    engine.print("info", "Handler 2 returned:", results[2][1]) -- 'handler_b'
end):resume()
```

```lua lineNumbers title="Multiple handlers each returning multiple values"
event.on("on_status", function() return "ok", 100 end)
event.on("on_status", function() return "ok", 200 end)

thread.create(function(self)
    local ok, results = self:await(event.emit_callback("on_status"))
    engine.print("info", results[1][1], results[1][2]) -- 'ok'  100
    engine.print("info", results[2][1], results[2][2]) -- 'ok'  200
end):resume()
```

```lua lineNumbers title="Send a callback from client to server and await the reply"
if engine.get_platform() == "client" then
    thread.create(function(self)
        local ok, result, data = self:await(event.emit_callback("on_fetch_inventory", { remote = true }, "weapons"))
        engine.print("info", "Server replied:", result) -- 'ok'
        engine.iprint(data)
    end):resume()
else
    event.on("on_fetch_inventory", function(category)
        local thread = thread.current()
        thread:sleep(2000)
        return "ok", { category = category, items = { "sword", "bow" } }
    end, { async = true })
end
```

```lua lineNumbers title="Send a callback from server to a specific client and await the reply"
if engine.get_platform() == "client" then
    event.on("on_request_position", function()
        local thread = thread.current()
        thread:sleep(500)
        return 12.5, 0.0, 87.3
    end, { async = true })
else
    thread.create(function(self)
        -- Single handler on client: resolves unpacked
        local ok, x, y, z = self:await(event.emit_callback("on_request_position", { remote = true, peer = targetPeerId }))
        engine.print("info", "Client position:", x, y, z)
    end):resume()
end
```

```lua lineNumbers title="Await aggregated results from multiple remote handlers"
if engine.get_platform() == "server" then
    event.on("on_status", function() return "ok" end)
    event.on("on_status", function() return "ok", { detail = "extra" } end)
else
    thread.create(function(self)
        -- Multiple handlers on server: resolves with {[1]={"ok"}, [2]={"ok", {detail="extra"}}}
        local ok, results = self:await(event.emit_callback("on_status", { remote = true }))
        engine.print("info", "Handler 1:", results[1][1])
        engine.print("info", "Handler 2:", results[2][1], results[2][2])
    end):resume()
end
```