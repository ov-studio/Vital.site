---
title: self:get_rotation
description: Retrieves the current rotation of the model in 3D space
badge: Shared
---

### Syntax

```lua
local rotation = self:get_rotation()
```

### Parameters

| Type | Name | Description |
|------|------|-------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector3` | rotation | Rotation of the model in 3D space as `{x, y, z}` (in radians) |

<hr/>

### Examples

```lua
local entity = model.create("character")
local rotation = entity:get_rotation()

engine.print("info", "Rotation:", rotation[1], rotation[2], rotation[3])
```
