---
title: self:to_local
description: Converts a world-space position to local-space relative to the point light
badge: client
---

### Syntax

```lua
local local_position = self:to_local(
    world_position
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | world_position | Position in world space as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector3` | local_position | Equivalent position in local space as `{x, y, z}` |

---

### Examples

```lua lineNumbers title="Convert a world position to the point light's local space"
local entity = light.point_light.create()
entity:set_position({10, 0, 0})

local local_pos = entity:to_local({10, 0, 5})
core.engine.print("info", util.table.unpack(local_pos))
```
