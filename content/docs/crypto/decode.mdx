---
title: crypto.decode
description: Decodes base64 string into equivalent buffer
badge: Shared
---

### Syntax

```lua
local buffer = crypto.decode(hash)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `string` | hash | Base64 hash to be decoded |


### Returns

| Type | Name | Description |
|------|------|-------------|
| `string` or `bool` | buffer | Decoded buffer from base64 hash on successful execution, or `false` on failure |

<hr/>

### Example

```lua
local input = "SGVsbG8gV29ybGQ="

engine.print("Input:", input)
engine.print("Decoded:", crypto.decode(input))
```