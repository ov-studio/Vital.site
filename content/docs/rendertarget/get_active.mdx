---
title: core.rendertarget.get_active
description: Retrieves the currently active rendertarget
badge: Client
---

### Syntax

```lua
local result = core.rendertarget.get_active()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `rendertarget` \| `bool` | result | Currently active rendertarget instance, `false` if none is set |

---

### Examples

```lua lineNumbers title="Get the currently active rendertarget"
local entity = core.rendertarget.get_active()

if entity then
    core.engine.print("info", "Active rendertarget size:", entity:get_size())
end
```
