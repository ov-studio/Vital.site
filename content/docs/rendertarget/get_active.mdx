---
title: rendertarget.get_active
description: Retrieves the currently active rendertarget
badge: Client
---

### Syntax

```lua
local result = rendertarget.get_active()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `rendertarget` \| `bool` | result | Currently active rendertarget instance, or `false` if none is set |

<hr/>

### Examples

```lua lineNumbers title="Get the currently active rendertarget"
local rt = rendertarget.get_active()

if rt then
    engine.print("info", "Active rendertarget size:", rt:get_size())
end
```
