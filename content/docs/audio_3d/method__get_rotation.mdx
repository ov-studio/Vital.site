---
title: self:get_rotation
description: Retrieves the rotation of the audio-3d instance in radians
badge: Client
---

### Syntax

```lua
local result = self:get_rotation()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `{float, float, float}` | result | Current Euler rotation in radians as `{x, y, z}` |

---

### Examples

```lua lineNumbers title="Get the rotation of an audio-3d instance"
local entity = core.audio_3d.create("sounds/ambient.ogg")

local rot = entity:get_rotation()
core.engine.print("info", rot[1], rot[2], rot[3])
```
