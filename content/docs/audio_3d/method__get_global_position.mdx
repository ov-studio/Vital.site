---
title: self:get_global_position
description: Retrieves the global world position of the audio-3d instance
badge: client
---

### Syntax

```lua
local result = self:get_global_position()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `{float, float, float}` | result | Current global position as `{x, y, z}` |

---

### Examples

```lua lineNumbers title="Get the world position of an audio-3d instance"
local entity = core.audio_3d.create("sounds/ambient.ogg")

local pos = entity:get_global_position()
core.engine.print("info", pos[1], pos[2], pos[3])
```
