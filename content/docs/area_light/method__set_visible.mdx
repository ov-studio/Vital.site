---
title: self:set_visible
description: Sets the visibility of the area light
badge: client
---

### Syntax

```lua
local status = self:set_visible(
    state
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `bool` | state | `true` to show the area light, `false` to hide it |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Hide a area light"
local entity = light.area_light.create()

entity:set_visible(false)
```
