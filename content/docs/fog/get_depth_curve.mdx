---
title: fog.get_depth_curve
description: Retrieves the current depth curve of the fog post-processing effect
badge: Client
---

### Syntax

```lua
local result = fog.get_depth_curve()
```

<Callout type="warn">
  **Only available when the fog mode is set to depth mode.** Refer [`fog.set_mode`](/docs/fog/set_mode) to configure the active fog mode.

  - **Depth mode required** — calling this function in any other fog mode will return an unexpected result
  - **Recommended usage** — use alongside [`fog.get_depth_begin`](/docs/fog/get_depth_begin) and [`fog.get_depth_end`](/docs/fog/get_depth_end) to inspect the complete fog transition profile
</Callout>

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `float` | result | Current depth curve value |

<hr/>

### Examples

```lua lineNumbers title="Print current fog depth curve"
engine.print("info", "Fog depth curve:", fog.get_depth_curve())
```
