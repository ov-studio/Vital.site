---
title: engine.get_resolution
description: Retrieves the current screen resolution in absolute pixels
badge: Client
---

### Syntax
```lua
local resolution = engine.get_resolution()
```


### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `vector2` | resolution | Screen resolution in pixels as `{width, height}` |

<hr/>

### Examples
```lua lineNumbers title="Draw a rectangle centered on the screen"
local resolution = engine.get_resolution()
local size = {200, 100}

network:fetch("vital.sandbox:draw", true):on(function()
    engine.draw_rectangle(
        {(resolution[1] - size[1])*0.5, (resolution[2] - size[2])*0.5},
        {size[1], size[2]},
        {1, 1, 1, 1}
    )
end)
```