---
title: engine.get_platform
description: Retrieves engine's current platform type
badge: Shared
---

### Syntax

```lua
local platform = engine.get_platform()
```

### Parameters

| No parameters are accepted by this function |
|---------------------------------------------|

### Returns

| Type | Name | Description |
|------|------|-------------|
| `string` | platform | Engine's current platform type:<br/>• `"client"` - indicates client-side execution<br/>• `"server"` - indicates server-side execution |

<hr/>

### Examples

```lua lineNumbers title="Prints engine's current platform type"
engine.print("info", "Platform Type:", engine.get_platform())
```
