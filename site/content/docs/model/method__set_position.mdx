---
title: self:set_position
description: Sets the position of the model
badge: shared
---

### Syntax

```lua
local status = self:set_position(
    position
)
```

### Parameters

| Type | Name | Description |
|------|------|-------------|
| `vector3` | position | World-space position to move the model to, as `{x, y, z}` |

### Returns

| Type | Name | Description |
|------|------|-------------|
| `bool` | status | `true` on successful execution, `false` otherwise |

---

### Examples

```lua lineNumbers title="Move a model to the world origin"
local entity = core.model.create("character")

entity:set_position({0, 0, 0})
```
